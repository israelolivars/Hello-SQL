SELECT * FROM users;                --selecciona todo de la tabla users

SELECT name FROM users;             --selecciona solo el campo name de la tabla users

SELECT user_id, name FROM users;    --selecciona user_id y name de la tabla users