SELECT * FROM users;                --selecciona todos los campos de la tabla users

SELECT name FROM users;             --selecciona el campo name de la tabla users

SELECT user_id, name FROM users;    --selecciona los campos: user_id y name de la tabla users